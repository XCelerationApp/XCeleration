Search the web for: $ARGUMENTS

## Steps

1. **Run a web search** using the WebSearch tool with the query: `$ARGUMENTS`

2. **Fetch the top results** — use WebFetch on the 2–3 most relevant URLs returned. Prioritise:
   - Official docs (flutter.dev, dart.dev, pub.dev, api.flutter.dev)
   - GitHub issues/PRs for bug-related queries
   - Stack Overflow for how-to questions
   - Blog posts or release notes for anything else

3. **Synthesise a concise answer** covering:
   - The direct answer to the query
   - Any caveats, version notes, or gotchas relevant to a Flutter/Dart project
   - Code snippets if they meaningfully illustrate the answer
   - Links to the 2–3 most useful sources at the end (labelled clearly)

Keep the response focused and skimmable. Do not dump raw search results — always digest them into a useful answer.
