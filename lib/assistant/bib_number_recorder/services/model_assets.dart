/// Paths to a downloaded sherpa-onnx model.
final class ModelAssets {
  const ModelAssets({required this.modelDir});

  /// Directory containing the four ONNX model files and tokens.txt.
  final String modelDir;
}
